import 'package:flutter/material.dart';

class SuggestionSearchList extends StatelessWidget {
  final String query;

  const SuggestionSearchList({
    Key? key,
    required this.query,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
